def swap1(a,b):

  #the only code which I know that can swap the values of 'a' & 'b'
  a = a ^ b
  b = a ^ b
  a = a ^ b
  print ("after swapping: a = ", a, " b = ", b)
swap1(50111000110, 51000111001)
